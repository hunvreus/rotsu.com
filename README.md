# rotsu.com

The Rotsu company site, built as a static Astro project with Tailwind CSS.

```sh
npm install
npm run dev
```

Production builds are deployed with Cloudflare Workers Static Assets. See [the deployment guide](docs/development/deployment.md).
